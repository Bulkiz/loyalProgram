spring:
  datasource:
    password: 12345
    url: jdbc:postgresql://localhost/loyal_program
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false