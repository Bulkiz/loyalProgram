spring:
  datasource:
    password: 5403
    url: jdbc:postgresql://localhost/loyal_program_test
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false